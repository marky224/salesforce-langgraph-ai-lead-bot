# Dev / test / tooling dependencies — NOT installed into the production image
# (the Dockerfile installs requirements.txt only). Install the full dev env with:
#     pip install -r backend/requirements-dev.txt
-r requirements.txt

# Test suite
pytest==8.4.2
pytest-asyncio==0.26.0

# Lint + type-check (versions mirrored in .github/workflows/ci.yml)
ruff==0.15.16
mypy==2.1.0
