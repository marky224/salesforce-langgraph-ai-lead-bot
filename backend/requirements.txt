# Core framework
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0
pydantic>=2.10.0,<3.0
pydantic-settings>=2.7.0,<3.0

# LangGraph / LangChain
langgraph>=0.4.0,<1.0
langchain-core>=0.3.0,<1.0
langgraph-checkpoint-postgres==3.0.5   # durable Postgres checkpointer (Neon); keep on 3.0.x — 3.1.0+ needs langgraph-checkpoint>=4 which breaks langgraph 0.6.x
psycopg[binary]==3.2.*                  # async driver; binary extra → no libpq in slim image

# LLM providers (uncomment the one you need)
# langchain-anthropic>=0.3.0,<1.0   # For Anthropic (Claude)
langchain-openai>=0.3.0,<1.0        # For OpenAI (GPT-4o) or xAI (Grok)
# langchain-groq>=0.2.0,<1.0        # For Groq (Llama/Mixtral)

# Salesforce
simple-salesforce>=1.12.0,<2.0

# Abuse protection (PR 2)
slowapi>=0.1.9,<1.0

# Email validation
email-validator>=2.0.0,<3.0

# Testing (dev only — excluded from Docker via .dockerignore)
pytest>=8.0.0,<9.0
pytest-asyncio>=0.24.0,<1.0
